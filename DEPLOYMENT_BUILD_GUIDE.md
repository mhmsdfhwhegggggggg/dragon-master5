# 🚀 FALCON Telegram Pro: دليل النشر والإطلاق
# Deployment & Build Guide

هذا الدليل يشرح خطوة بخطوة كيفية رفع السيرفر مجانًا وبناء تطبيق الأندرويد (APK) للنشر.
(This guide explains step-by-step how to deploy the server for free and build the Android APK.)

---

## الجزء الأول: رفع السيرفر (Part 1: Server Deployment)
سنستخدم خدمة **Render** لأنها توفر استضافة مجانية لـ Node.js و Docker.

### 1. تجهيز الكود (Prepare Code)
تأكد من أن الكود الخاص بك مرفوع على **GitHub** أو **GitLab**.
(Ensure your code is pushed to GitHub/GitLab).

### 2. التسجيل في Render (Sign up)
1.  اذهب إلى [render.com](https://render.com).
2.  سجل حسابًا جديدًا (Sign Up) باستخدام GitHub.

### 3. إنشاء خدمة جديدة (Create Web Service)
1.  اضغط على **"New +"** ثم **"Web Service"**.
2.  اختر المستودع (Repository) الخاص بمشروعك (Dragaan/Falcon).
3.  **الإعدادات (Settings)**:
    *   **Name**: اختر اسماً (مثلاً: `falcon-server`).
    *   **Region**: Frankfurt (الأقرب) أو أي منطقة تفضلها.
    *   **Branch**: `main` (أو الفرع الذي تعمل عليه).
    *   **Runtime**: `Docker`.
    *   **Instance Type**: `Free`.

### 4. المتغيرات البيئية (Environment Variables)
في صفحة الإعدادات، ابحث عن قسم **"Environment Variables"** وأضف التالي:

| Key | Value | ملاحظات |
| :--- | :--- | :--- |
| `NODE_ENV` | `production` | أساسي |
| `DATABASE_URL` | `postgresql://...` | رابط قاعدة البيانات (يمكنك إنشاء واحدة مجانية على Render أو Supabase) |
| `REDIS_URL` | `redis://...` | رابط Redis (اختياري للنسخة المجانية، لكن يفضل وجوده) |
| `TELEGRAM_API_ID` | `123456` | من my.telegram.org |
| `TELEGRAM_API_HASH` | `abcdef...` | من my.telegram.org |
| `JWT_SECRET` | `(أي نص عشوائي طويل)` | للأمان |
| `ENCRYPTION_KEY` | `(32 حرف بالضبط)` | لتشفير الجلسات (12345678901234567890123456789012) |

### 5. الانتهاء (Finish)
اضغط **"Create Web Service"**. سينتظر Render قليلًا ثم يبدأ البناء. إذا ظهرت علامة ✅ خضراء، فالسيرفر يعمل!
**انسخ رابط السيرفر** (مثلاً: `https://falcon-server.onrender.com`)، سنحتاجه في التطبيق.

---

## الجزء الثاني: بناء التطبيق (Part 2: Build APK)
سنستخدم خدمة **Expo EAS** لبناء التطبيق في السحابة بدون الحاجة لكمبيوتر قوي.

### 1. تثبيت الأدوات (Install Tools)
افتح التيرمينال (Terminal) في فيجوال ستوديو كود واكتب:
```bash
npm install -g eas-cli
```

### 2. تسجيل الدخول (Login)
```bash
eas login
```
(سجل دخولك بحساب Expo الخاص بك، أو أنشئ حسابًا جديدًا إذا لم يكن لديك).

### 3. إعداد المشروع (Configure Project)
تأكد من أن ملف `app.json` يحتوي على `slug` فريد (اسم المشروع). ثم نفذ:
```bash
eas build:configure
```
*   اختر **Android**.

### 4. ربط السيرفر بالتطبيق (Connect Server)
قبل البناء، يجب إخبار التطبيق برابط السيرفر الذي أنشأته في الخطوة الأولى.
افتح ملف `.env` (أو `env.ts` إذا كان مدمجاً) وتأكد من وضع رابط السيرفر:
```typescript
// في الكود، تأكد أن التطبيق يتصل بـ:
// https://falcon-server.onrender.com
// بدلاً من localhost
```
*ملاحظة: في FALCON، يتم تحديد الرابط غالبًا في `app.config.ts` أو `constants`. سنقوم بتحديثه لك إذا لزم الأمر.*

### 5. بدء البناء (Start Build)
لبناء ملف **APK** قابل للتثبيت المباشر على الهواتف:
```bash
eas build -p android --profile preview
```
*   سيسألك عما إذ كنت تريد إنشاء "Keystore" جديد؟ اختر **Yes** (لأول مرة).
*   انتظر... سيقوم السيرفر برفع الكود وبنائه.
*   عند الانتهاء، سيعطيك **رابط تحميل مباشر** لملف الـ APK 📥.

---

## الجزء الثالث: النشر (Part 3: Publish)
1.  حمّل ملف APK من الرابط.
2.  انشره للمستخدمين عبر قناتك في تيليجرام أو موقعك.
3.  تأكد من أن المستخدمين قاموا بتفعيل "تثبيت التطبيقات من مصادر غير معروفة" إذا لم يكن التطبيق على المتجر.

---

## 🛠 يحتاج مساعدة؟ (Troubleshooting)
*   **خطأ في البناء؟** تأكد من مسح مجلد `node_modules` وإعادة التثبيت (`npm install`) قبل الرفع.
*   **السيرفر لا يعمل؟** راجع "Logs" في لوحة تحكم Render.
