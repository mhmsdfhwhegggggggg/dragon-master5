# دليل النشر الكامل - Dragon Telegram Pro Mobile

## تاريخ: 9 فبراير 2026

---

## 🚀 خيارات النشر المتاحة

### الخيار 1: Render.com (موصى به) ⭐
**المميزات:**
- ✅ استضافة مجانية 24/7
- ✅ دعم Node.js كامل
- ✅ PostgreSQL مجاني (256 MB)
- ✅ Redis مجاني
- ✅ SSL تلقائي (HTTPS)
- ✅ نشر تلقائي من GitHub
- ✅ سهل جداً في الإعداد

**العيوب:**
- ⚠️ ينام بعد 15 دقيقة من عدم النشاط (الخطة المجانية)
- ⚠️ موارد محدودة (512 MB RAM)

**خطوات النشر:**

1. **إنشاء حساب على Render.com**
   - اذهب إلى https://render.com
   - سجل حساب جديد (يمكنك استخدام GitHub)

2. **ربط المستودع**
   - اضغط على "New +" → "Blueprint"
   - اختر المستودع: `mhmsdfhwhegggggggg/dragaan`
   - Render سيكتشف ملف `render-deploy.yaml` تلقائياً

3. **تكوين المتغيرات البيئية**
   - سيتم إنشاء قاعدة البيانات و Redis تلقائياً
   - أضف المتغيرات التالية يدوياً:
     ```
     TELEGRAM_API_ID=your_api_id
     TELEGRAM_API_HASH=your_api_hash
     OAUTH_SERVER_URL=https://oauth.manus.im
     VITE_APP_ID=your_app_id
     OWNER_OPEN_ID=your_owner_id
     ```

4. **النشر**
   - اضغط "Apply"
   - انتظر حتى يكتمل البناء (3-5 دقائق)
   - ستحصل على رابط مثل: `https://dragaan-api.onrender.com`

---

### الخيار 2: Railway.app
**المميزات:**
- ✅ استضافة مجانية ($5 رصيد شهري)
- ✅ لا ينام (يعمل 24/7)
- ✅ دعم PostgreSQL و Redis
- ✅ نشر سريع جداً

**العيوب:**
- ⚠️ الرصيد المجاني محدود ($5/شهر)
- ⚠️ قد ينتهي الرصيد قبل نهاية الشهر

**خطوات النشر:**

1. **إنشاء حساب على Railway.app**
   - اذهب إلى https://railway.app
   - سجل حساب جديد

2. **إنشاء مشروع جديد**
   - اضغط "New Project"
   - اختر "Deploy from GitHub repo"
   - اختر المستودع: `mhmsdfhwhegggggggg/dragaan`

3. **إضافة قاعدة البيانات و Redis**
   - اضغط "+ New" → "Database" → "Add PostgreSQL"
   - اضغط "+ New" → "Database" → "Add Redis"

4. **تكوين المتغيرات**
   - افتح إعدادات الخدمة
   - أضف جميع المتغيرات من `.env.production`

5. **النشر**
   - Railway سينشر تلقائياً
   - ستحصل على رابط مثل: `https://dragaan-production.up.railway.app`

---

### الخيار 3: Fly.io
**المميزات:**
- ✅ استضافة مجانية سخية
- ✅ يعمل 24/7
- ✅ أداء ممتاز
- ✅ دعم Docker

**العيوب:**
- ⚠️ يحتاج بطاقة ائتمان للتحقق
- ⚠️ إعداد أكثر تعقيداً

**خطوات النشر:**

1. **تثبيت Fly CLI**
   ```bash
   curl -L https://fly.io/install.sh | sh
   ```

2. **تسجيل الدخول**
   ```bash
   fly auth login
   ```

3. **إنشاء تطبيق**
   ```bash
   cd dragaan
   fly launch --name dragaan-pro
   ```

4. **إضافة PostgreSQL**
   ```bash
   fly postgres create --name dragaan-db
   fly postgres attach dragaan-db
   ```

5. **إضافة Redis**
   ```bash
   fly redis create --name dragaan-redis
   ```

6. **تكوين المتغيرات**
   ```bash
   fly secrets set TELEGRAM_API_ID=your_id
   fly secrets set TELEGRAM_API_HASH=your_hash
   ```

7. **النشر**
   ```bash
   fly deploy
   ```

---

### الخيار 4: Heroku
**المميزات:**
- ✅ معروف وموثوق
- ✅ دعم كامل للإضافات
- ✅ سهل الاستخدام

**العيوب:**
- ❌ لا توجد خطة مجانية بعد الآن
- ⚠️ يحتاج بطاقة ائتمان

**خطوات النشر:**

1. **تثبيت Heroku CLI**
   ```bash
   curl https://cli-assets.heroku.com/install.sh | sh
   ```

2. **تسجيل الدخول**
   ```bash
   heroku login
   ```

3. **إنشاء تطبيق**
   ```bash
   cd dragaan
   heroku create dragaan-pro
   ```

4. **إضافة PostgreSQL و Redis**
   ```bash
   heroku addons:create heroku-postgresql:mini
   heroku addons:create heroku-redis:mini
   ```

5. **تكوين المتغيرات**
   ```bash
   heroku config:set TELEGRAM_API_ID=your_id
   heroku config:set TELEGRAM_API_HASH=your_hash
   ```

6. **النشر**
   ```bash
   git push heroku main
   ```

---

## 🔧 إعداد قاعدة البيانات بعد النشر

بعد نشر التطبيق، يجب تطبيق migrations:

### عبر Render.com
1. افتح Shell في لوحة التحكم
2. نفذ:
   ```bash
   pnpm db:push
   ```

### عبر Railway.app
1. افتح Terminal في لوحة التحكم
2. نفذ:
   ```bash
   pnpm db:push
   ```

### عبر Fly.io
```bash
fly ssh console
pnpm db:push
```

---

## 🌐 الحصول على بيانات اعتماد Telegram API

1. **اذهب إلى:** https://my.telegram.org/apps
2. **سجل الدخول** برقم هاتفك
3. **أنشئ تطبيق جديد:**
   - App title: Dragon Telegram Pro
   - Short name: dragaan
   - Platform: Android
4. **احفظ:**
   - `api_id` (رقم)
   - `api_hash` (نص)

---

## 📱 ربط التطبيق الجوال بالسيرفر

بعد نشر السيرفر، عدّل ملف التكوين في التطبيق:

### ملف: `lib/_core/api-client.ts`

```typescript
const API_URL = process.env.EXPO_PUBLIC_API_URL || 'https://your-server-url.onrender.com';
```

أو أضف في `.env`:
```
EXPO_PUBLIC_API_URL=https://dragaan-api.onrender.com
```

---

## 🔐 تأمين التطبيق

### 1. تغيير المفاتيح السرية
لا تستخدم المفاتيح الافتراضية! قم بتوليد مفاتيح جديدة:

```bash
# JWT Secret (32 حرف على الأقل)
openssl rand -base64 32

# Session Secret
openssl rand -base64 32

# Encryption Key (بالضبط 32 حرف)
openssl rand -hex 16
```

### 2. تفعيل HTTPS فقط
في ملف `.env.production`:
```
FORCE_HTTPS=true
```

### 3. تكوين CORS
```
CORS_ORIGINS=https://yourdomain.com,https://app.yourdomain.com
```

### 4. Rate Limiting
```
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
```

---

## 📊 مراقبة التطبيق

### Render.com
- افتح Dashboard → Logs
- راقب الأخطاء والأداء

### Railway.app
- افتح Deployments → Logs
- راقب الاستخدام والرصيد

### Fly.io
```bash
fly logs
fly status
```

---

## 🐛 حل المشاكل الشائعة

### المشكلة 1: السيرفر لا يبدأ
**الحل:**
- تحقق من Logs
- تأكد من وجود جميع المتغيرات البيئية
- تأكد من تطبيق migrations

### المشكلة 2: خطأ في الاتصال بقاعدة البيانات
**الحل:**
- تحقق من `DATABASE_URL`
- تأكد من إنشاء قاعدة البيانات
- نفذ `pnpm db:push`

### المشكلة 3: خطأ في Redis
**الحل:**
- تحقق من `REDIS_URL`
- تأكد من تشغيل Redis
- أعد تشغيل السيرفر

### المشكلة 4: Telegram API لا يعمل
**الحل:**
- تحقق من `TELEGRAM_API_ID` و `TELEGRAM_API_HASH`
- تأكد من صحة البيانات من my.telegram.org
- تحقق من الاتصال بالإنترنت

---

## ✅ قائمة التحقق قبل الإنتاج

- [ ] تم تغيير جميع المفاتيح السرية
- [ ] تم إضافة بيانات Telegram API
- [ ] تم تطبيق migrations
- [ ] تم اختبار جميع الوظائف
- [ ] تم تكوين CORS بشكل صحيح
- [ ] تم تفعيل Rate Limiting
- [ ] تم تفعيل HTTPS
- [ ] تم إعداد النسخ الاحتياطي
- [ ] تم إعداد المراقبة
- [ ] تم توثيق API

---

## 🎯 الخلاصة

**الخيار الموصى به للبدء:** Render.com

**السبب:**
- سهل جداً في الإعداد
- مجاني تماماً
- يعمل 24/7 (مع إمكانية النوم)
- دعم ممتاز

**للإنتاج الحقيقي:** Railway.app أو Fly.io

**السبب:**
- لا ينام
- أداء أفضل
- موارد أكثر
- موثوقية أعلى

---

## 📞 الدعم

إذا واجهت أي مشاكل:
1. راجع Logs
2. تحقق من المتغيرات البيئية
3. راجع التوثيق الرسمي للخدمة
4. افتح Issue في GitHub

---

**ملاحظة مهمة:** 
جميع الخدمات المذكورة تتطلب التحقق من الهوية (بطاقة ائتمان أو رقم هاتف) للخطط المجانية. هذا للحماية من إساءة الاستخدام.
