# 🚀 دليل احتراف التشغيل والإنتاج (Mastery Guide) - FALCON Pro

هذا الدليل هو مرجعك الشامل للانتقال بنظام **FALCON** من بيئة التطوير إلى التشغيل الحقيقي المستقر والمربح على السيرفر (Linux VPS/Dedicated).

---

## 🏗️ المرحلة الأولى: تجهيز السيرفر (Server Preparation)

### 1. المتطلبات العتادية (Hardware)
لتشغيل النظام بكفاءة عالية (خاصة مع تفعيل الـ AI والـ Cloner)، يُنصح بالمواصفات التالية:
- **المعالج**: 4 نوى (CPU Cores) على الأقل.
- **الذاكرة**: 8 جيجا رام (RAM) - لضمان ثبات الـ Worker والـ ML Engine.
- **التخزين**: 50 جيجا SSD.

### 2. المتطلبات البرمجية (Software Stack)
يجب تثبيت النسخ التالية لضمان التوافق التام:
- **Node.js**: v22 (LTS) أو أحدث.
- **PostgreSQL**: v16 (قاعدة البيانات الأساسية).
- **Redis**: v7 (لإدارة الطوابير والسرعة).
- **PM2**: لإدارة العمليات وضمان عدم توقف السيرفر.

---

## 🚢 المرحلة الثانية: الرفع والتشغيل (Deployment Strategy)

### 1. إعداد البيئة (Environment Setup)
قم بإنشاء ملف `.env` في السيرفر وتأكد من تغيير القيم التالية فوراً:
```env
# الأمان (هام جداً!)
JWT_SECRET=قم_بتوليد_سلسلة_عشوائية_طويلة_جداً
ENCRYPTION_KEY=يجب_أن_يكون_32_حرفاً_بالضبط_لحماية_حسابات_تليجرام

# قاعدة البيانات
DATABASE_URL=postgresql://user:password@localhost:5432/falcon_db

# تليجرام API
TELEGRAM_API_ID=XXXXX
TELEGRAM_API_HASH=XXXXXXXXXXXXXXX
```

### 2. خطة التشغيل (Startup Sequence)
نفذ الأوامر التالية بالترتيب داخل مجلد المشروع في السيرفر:
```bash
# 1. تثبيت الحزم
npm install

# 2. بناء الأكواد
npm run build

# 3. تحديث هيكل قاعدة البيانات
npx drizzle-kit push

# 4. التشغيل عبر PM2 لضمان الاستمرارية
pm2 start dist/_core/index.js --name "falcon-api"
pm2 start dist/worker.js --name "falcon-worker"
```

---

## 🛡️ المرحلة الثالثة: الإدارة المتقدمة (Real Production Management)

### 1. ضبط الـ Anti-Ban للتشغيل الكثيف
النظام مجهز بـ **ML Engine** (محرك تعلم آلي) لتقليل الحظر. للإدارة الاحترافية:
- **التأخير الذكي**: لا تجعل الفاصل الزمني بين الرسائل أقل من 2000-3000ms في البداية.
- **التدرج**: ابدأ بحسابات "قديمة" (Aged Accounts) لرفع الثقة قبل استخدام الحسابات الجديدة.
- **البروكسي (Proxy)**: استخدم بروكسيات من نوع SOCKS5 لتوزيع الأحمال وتجنب حظر الـ IP الخاص بالسيرفر.

### 2. نظام التراخيص (Commercial Licensing)
إذا كنت تخطط لبيع النظام للعملاء:
- النظام يدعم إصدار مفاتيح (Keys) مربوطة بـ `Hardware ID`.
- يمكنك استخدام لوحة التحكم في `app/(tabs)/license-dashboard.tsx` لمراقبة التراخيص المفعلة.

---

## 📊 المرحلة الرابعة: المراقبة والصيانة (Monitoring)

### 1. مراقبة الصحة (Health Checks)
استخدم الروابط التالية للتأكد من أن السيرفر يعمل:
- `http://your-server-ip:3000/health`: لفحص حالة الـ API.
- `pm2 monit`: لمراقبة استهلاك المعالج والذاكرة في الوقت الحقيقي.

### 2. النسخ الاحتياطي (Backups)
من الضروري جدولة نسخة احتياطية يومية لقاعدة البيانات:
```bash
pg_dump falcon_db > backups/backup_$(date +%F).sql
```

---

## 💡 نصائح للنجاح في الإنتاج الحقيقي
1. **استخدام Nginx**: لا تفتح منفذ 3000 مباشرة، استخدم Nginx كـ Reverse Proxy وقم بتفعيل SSL (HTTPS).
2. **سجلات الأخطاء**: تابع `pm2 logs` بشكل دوري لاكتشاف أي محاولات اختراق أو أخطاء في الـ API.
3. **تحديث الـ Worker**: إذا زاد عدد عمليات الـ Cloning، يمكنك تشغيل أكثر من Worker instance عبر PM2 لزيادة الإنتاجية.

---
**تم إعداد هذا الدليل لضمان أقصى درجات الاستقرار والأمان لنظام FALCON Pro.** 🚀
