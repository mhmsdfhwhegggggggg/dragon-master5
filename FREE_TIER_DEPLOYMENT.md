# 💸 دليل النشر المجاني بالكامل (Free Tier Guide)

هذا الدليل يشرح كيفية رفع المشروع **مجاناً 100%** باستخدام أفضل الخدمات المتاحة حالياً.

---

## 🏗️ الهيكلية المجانية

سنستخدم 3 خدمات مختلفة (لأن Render لم يعد يوفر قواعد بيانات مجانية دائمة):
1.  **Render**: لاستضافة الكود (Node.js API).
2.  **Neon.tech**: لاستضافة قاعدة البيانات (PostgreSQL) - يعطي 0.5GB مجاناً.
3.  **Upstash**: لاستضافة Redis - يعطي 10,000 طلب يومياً مجاناً.

---

## 🚀 الخطوة 1: إعداد قاعدة البيانات (Neon)

1. اذهب إلى [Neon.tech](https://neon.tech) وسجل حساباً.
2. أنشئ مشروعاً جديداً باسم `dragaan-db`.
3. انسخ **Connection String** (تأكد من اختيار `Pooled connection` إذا كان متاحاً).
   - الصيغة تكون: `postgresql://user:password@ep-xyz.us-east-2.aws.neon.tech/dbname?sslmode=require`
   - احتفظ بهذا الرابط، سنسميه `DATABASE_URL`.

## ⚡ الخطوة 2: إعداد Redis (Upstash)

1. اذهب إلى [Upstash.com](https://upstash.com) وسجل حساباً.
2. أنشئ قاعدة بيانات Redis جديدة.
3. انسخ رابط الاتصال من قسم "Connect".
   - الصيغة تكون: `redis://default:password@fly-xyz.upstash.io:6379`
   - احتفظ بهذا الرابط، سنسميه `REDIS_URL`.

---

## ☁️ الخطوة 3: الرفع على Render

1. اذهب إلى [dashboard.render.com](https://dashboard.render.com).
2. اختر **New +** ثم **Blueprints**.
3. قم بربط حساب GitHub الخاص بك واختر هذا المستودع `dragaan`.
4. واسمح له بقراءة ملف `render.yaml`.
5. سيطلب منك Render الموافقة على إنشاء خدمة `dragon-telegram-pro` (مجانية).
6. **هام جداً:** في الخطوة التالية، سيطلب منك إدخال "Environment Variables" المفقودة:
   
   | المتغير | القيمة |
   |---------|--------|
   | `DATABASE_URL` | ضع رابط Neon الذي نسخته في الخطوة 1 |
   | `REDIS_URL` | ضع رابط Upstash الذي نسخته في الخطوة 2 |
   | `TELEGRAM_API_ID` | رقم الـ API الخاص بـ Telegram |
   | `TELEGRAM_API_HASH` | كود الـ Hash الخاص بـ Telegram |

7. اضغط **Apply**.

---

## ✅ التحقق من العمل

بعد الانتهاء من النشر (قد يستغرق 5-10 دقائق):
1. سيظهر لك رابط التطبيق (مثلاً `https://dragon-telegram-pro.onrender.com`).
2. جرب الدخول على الرابط `/api/health`. إذا ظهرت لك رسالة `{"status":"ok"}`، فكل شيء يعمل بنجاح!

---
**ملاحظة:** لقد قمتُ بتعديل إعدادات المشروع ليعمل الـ API والـ Worker في "حاوية واحدة" (Container) لتوفير التكاليف والبقاء في الخطة المجانية.
