# 🎮 FALCON Telegram Pro: دليل التشغيل والإدارة
# Operational & Administration Guide

بعد رفع السيرفر (Deployment) وبناء التطبيق، ستحتاج إلى القيام بخطوات "اليوم الأول" لتشغيل النظام بشكل صحيح.
(After deployment, you need to perform "Day 1" steps to initialize the system correctly.)

---

## 1️⃣ تهيئة قاعدة البيانات (Database Initialization)
عند تشغيل السيرفر لأول مرة، تكون قاعدة البيانات فارغة. يجب إنشاء الجداول (Tables).

### الخطوات (Steps):
1.  ادخل إلى لوحة تحكم **Render**.
2.  اذهب إلى تبويب **"Shell"** في خدمة الويب الخاصة بك.
3.  اكتب الأمر التالي واضغط Enter:
    ```bash
    npm run db:push
    ```
    *   ✅ سيقوم هذا الأمر بإنشاء جميع الجداول اللازمة في قاعدة البيانات.

---

## 5️⃣ إعداد Redis (Setting up Redis) prince
نظام **"نبض القلب" (Heart-Beat)** وخصائص الحماية المتقدمة تعتمد على **Redis** لتتبع محاولات الحظر وتوزيع الأحمال.

### كيفية إنشاء Redis على Render: prince
1.  اضغط **New +** ثم اختر **Redis**.
2.  أعطِ اسماً للخدمة (مثلاً: `falcon-redis`).
3.  اختر الخطة المجانية (**Free Plan**).
4.  بعد الإنشاء، ستجد حقل اسمه **Internal Redis URL**.
5.  انسخ هذا الرابط (يبدأ بـ `redis://...`).
6.  اذهب إلى إعدادات خدمة الويب الخاصة بك (Server) -> **Environment Variables**.
7.  أضف متغيراً جديداً:
    *   **Key**: `REDIS_URL`
    *   **Value**: (الرابط الذي نسخته)
### الخطة البديلة: استخدام Upstash (إذا لم يظهر Redis في Render) prince
إذا لم تجد خيار Redis في Render، يمكنك استخدام **Upstash** وهو مجاني تماماً ويعمل بشكل ممتاز:
1.  اذهب إلى [upstash.com](https://upstash.com) وسجل دخولك بجوجل أو جيت هب.
2.  اضغط على **Create Database**.
3.  اختر اسماً (مثلاً: `falcon-security`) واضغط **Create**.
4.  في صفحة قاعدة البيانات، انزل للأسفل حتى تجد **Redis Connect**.
5.  انسخ الرابط الذي يبدأ بـ `redis://...`.
6.  ضعه في Render كمتغير بيئي باسم `REDIS_URL` كما شرحنا سابقاً. Prince.

---

## 1️⃣ تهيئة قاعدة البيانات (Automatic Database Setup) prince
لقد جعلتُ هذه الخطوة **آلية بالكامل**. عند تشغيل السيرفر، سيقوم بإنشاء جميع الجداول اللازمة تلقائياً. لا داعي لاستخدام "Shell".

---

## 2️⃣ إنشاء حساب المدير (Self-Healing Admin) prince
نظراً لأن Render قد يطلب بطاقة لاستخدام "Shell"، فقد قمتُ ببرمجة النظام ليقوم بإنشاء حساب مدير افتراضي **تلقائياً** عند أول تشغيل.

### بيانات الدخول الافتراضية (Default Login): prince
*   **الإيميل**: `admin@falcon.pro`
*   **كلمة المرور**: `falcon_heart_2026`
*   **الاسم**: `Falcon Admin`

> [!TIP]
> يمكنك تغيير هذه البيانات من خلال لوحة التحكم داخل التطبيق بعد تسجيل الدخول، أو عبر إضافة متغيرات بيئية (`ADMIN_EMAIL` و `ADMIN_PASSWORD`) في Render وسيقوم النظام بتحديثها تلقائياً. Prince.

---

## 3️⃣ إدارة النظام (System Administration)
الآن بعد أن أصبح لديك حساب مدير، يمكنك إدارة كل شيء من التطبيق أو لوحة التحكم.

### تسجيل الدخول (Login)
*   افتح تطبيق **FALCON** الذي قمت بتثبيته.
*   استخدم البريد الإلكتروني وكلمة المرور التي أنشأتها للتو.

### الصلاحيات (Permissions)
*   بصفتك "Admin"، لديك صلاحيات كاملة لرؤية جميع المستخدمين، التراخيص، وإحصائيات النظام.
*   يمكنك إنشاء تراخيص جديدة (Licenses) للمستخدمين الآخرين من لوحة التحكم داخل التطبيق.

---

## 4️⃣ استكشاف الأخطاء وإصلاحها (Troubleshooting)

### نسيت كلمة المرور؟
*   يمكنك دائمًا إعادة تشغيل أمر `create-admin.ts` بنفس الإيميل وكلمة مرور *جديدة*، وسيقوم النظام بتحديثها لك.

### السيرفر لا يعمل؟
*   تأكد من أن `DATABASE_URL` في إعدادات Render صحيح.
*   تأكد من أنك قمت بتشغيل `npm run db:push`.
