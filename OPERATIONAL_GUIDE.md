# 🎮 FALCON Telegram Pro: دليل التشغيل والإدارة
# Operational & Administration Guide

بعد رفع السيرفر (Deployment) وبناء التطبيق، ستحتاج إلى القيام بخطوات "اليوم الأول" لتشغيل النظام بشكل صحيح.
(After deployment, you need to perform "Day 1" steps to initialize the system correctly.)

---

## 1️⃣ تهيئة قاعدة البيانات (Database Initialization)
عند تشغيل السيرفر لأول مرة، تكون قاعدة البيانات فارغة. يجب إنشاء الجداول (Tables).

### الخطوات (Steps):
1.  ادخل إلى لوحة تحكم **Render**.
2.  اذهب إلى تبويب **"Shell"** في خدمة الويب الخاصة بك.
3.  اكتب الأمر التالي واضغط Enter:
    ```bash
    npm run db:push
    ```
    *   ✅ سيقوم هذا الأمر بإنشاء جميع الجداول اللازمة في قاعدة البيانات.

---

## 2️⃣ إنشاء حساب المدير (Create Admin Account)
لا يوجد حساب مدير افتراضي لأسباب أمنية. يجب عليك إنشاء واحد يدويًا.

### الخطوات (Steps):
1.  في نفس تبويب **"Shell"** في Render.
2.  اكتب الأمر التالي لإنشاء مدير جديد:
    ```bash
    npx tsx scripts/create-admin.ts admin@falcon.pro strong_password AdminUser
    ```
    *(استبدل `admin@falcon.pro` بإيميلك، و `strong_password` بكلمة مرور قوية)*.

    *   ✅ ستظهر رسالة: `Admin user created successfully!`

---

## 3️⃣ إدارة النظام (System Administration)
الآن بعد أن أصبح لديك حساب مدير، يمكنك إدارة كل شيء من التطبيق أو لوحة التحكم.

### تسجيل الدخول (Login)
*   افتح تطبيق **FALCON** الذي قمت بتثبيته.
*   استخدم البريد الإلكتروني وكلمة المرور التي أنشأتها للتو.

### الصلاحيات (Permissions)
*   بصفتك "Admin"، لديك صلاحيات كاملة لرؤية جميع المستخدمين، التراخيص، وإحصائيات النظام.
*   يمكنك إنشاء تراخيص جديدة (Licenses) للمستخدمين الآخرين من لوحة التحكم داخل التطبيق.

---

## 4️⃣ استكشاف الأخطاء وإصلاحها (Troubleshooting)

### نسيت كلمة المرور؟
*   يمكنك دائمًا إعادة تشغيل أمر `create-admin.ts` بنفس الإيميل وكلمة مرور *جديدة*، وسيقوم النظام بتحديثها لك.

### السيرفر لا يعمل؟
*   تأكد من أن `DATABASE_URL` في إعدادات Render صحيح.
*   تأكد من أنك قمت بتشغيل `npm run db:push`.
